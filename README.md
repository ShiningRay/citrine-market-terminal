# Citrine 行情终端（模拟盘）

用 [Citrine](https://github.com/ShiningRay/citrine)（信号式 Ruby UI 框架，经 Opal 编译到浏览器）
写的一个**完整的行情终端 / 投资组合管理 demo**：10 只 A 股模拟标的、逐档行情、
纯 `div` 绘制的蜡烛图与分时图、限价挂单撮合、T+1 与手续费规则、账户统计与权益曲线。
业务逻辑全部是纯 Ruby 且可在 CRuby 下单测，UI 只是它的一个渲染目标。

> 这是 Citrine 的**压力测试应用**：用它把框架在真实规模下的摩擦一条条撞出来。
> 结论都记在 **[FRICTION.md](FRICTION.md)**（25 条，含源码定位与改法建议），
> 另附一份独立审计报告 [docs/citrine-v1-audit.md](docs/citrine-v1-audit.md)。
>
> 2026-09-14 第一次迁移：框架侧 F4（响应式属性）/ F7（生命周期）/ F10（键盘）/
> F12–F13（`Citrine::Num`）落地后"**把外挂层交还给框架**"——原 128 行的
> `app/browser_glue.rb` 只剩 `app/test_api.rb`，自选/持仓面板的外观更新不再重建节点。
>
> 2026-09-14 第二次迁移：框架侧 **F5 组件嵌套 + F6 keyed 复用**落地后（citrine PR #15）
> "**把组件树还给组件树**"——8 个 view mixin 变成 12 个真正的子组件（8 面板 + 4 行组件），
> `Terminal` 只留全局模型状态，列表行改成带 key 的子组件（重排 = 移动真实节点），
> "输入框所在的块不读信号"这类绕法纪律随之退休。迁移前后**每档重跑/新建节点逐项相同**
> （140 次重跑 / 3 个新建节点，同 seed 对照）。
> 两次迁移记录都在 FRICTION.md **第七节**（第二次见 7.6，含一次真机截图才看出来的布局事故）。

```
┌─ 顶部：交易时段 / 第 N 档 · 总资产 / 浮动盈亏 / 累计收益率 · 暂停 1x 2x 4x · 自动交易 · 快捷键 ─┐
├── 自选行情（10 只，可排序）──┬── 行情走势（蜡烛/分时 + SMA + 成交量）──┬── 交易下单 ──────────┤
│  最新 / 涨跌 / 涨跌幅 / 量额 │  行情快照 + 技术指标（SMA/RSI/波动/形态） │  市价/限价 · 数量快捷 │
├── 账户统计 + 权益曲线 ───────┤  持仓（实时市值 / 浮盈 / 平仓）──────────┤  成交记录 / 挂单 ────┤
└───────────────────────── 信号与渲染埋点（每档 Effect 重跑数 / 新建节点数 / 耗时）──────────┘
```

## 运行

```bash
# 依赖：Ruby ≥ 3.0、Opal 1.8（gem install opal）、Node（仅验收用）
# 需要 citrine 仓库与本仓库同级，或用 CITRINE_ROOT 指定

bin/dev                 # → http://localhost:4403/market.html
                        #    改 app/**/*.rb 或 citrine/lib/**/*.rb 都会自动刷新

rake check              # 单测 + 桩验收 + 跨平台一致性（提交前跑）
```

| 命令 | 作用 |
|---|---|
| `bin/dev` | 开发服务器（包装 citrine 的 dev server，端口 4403，支持 `-p` 覆盖） |
| `rake test` | CRuby 内核单测（29 项 / 238 断言：引擎、指标、账户、交易规则） |
| `rake build` | 编译 `app/market.rb` → `app/market.js` |
| `rake stubs` | Node DOM 桩验收（98 项交互断言，不需要浏览器） |
| `rake parity` | CRuby 与 Opal 两侧内核输出**逐字节**比对（51 行） |
| `rake check` | 以上全部 |

## 操作

| 操作 | 方式 |
|---|---|
| 切标的 | 点自选行 / `↑` `↓` |
| 暂停 / 继续 | `⏸` 按钮 / 空格 |
| 变速 | `1x` `2x` `4x` / 数字键 `1` `2` `3` |
| 买入 / 卖出 | `买入`/`卖出` chip / `B` `S` 键 |
| 下单 | 填数量（100 股整手）→ 提交按钮 / 输入框外按 `Enter` |
| 全仓 / 清仓 | 数量区「全部」/ 持仓区「一键清仓」 |
| 挂单 / 撤单 | 切到「限价」填价格提交；「挂单」页撤单 |
| 自动交易 | 顶部开关（每 5 档随机下单，含卖出已持仓） |
| 清提示 | `Esc` |

## 目录

```
├── app/
│   ├── market.rb          # 浏览器入口：挂载 + 装埋点 + 暴露验收 API（心跳/快捷键都在组件上）
│   ├── market.html        # 页面外壳（挂载点 + 外链 styles.css；令牌在 tokens.rb）
│   ├── tokens.rb          # 设计令牌唯一来源（浏览器 CSS / 视图 / 原生 theme 三处共用）
│   ├── styles.css         # 浏览器样式表（规则经 var(--x) 引用令牌，无字面量色值）
│   ├── terminal.rb        # 根组件：全局模型状态、computed 链、心跳（on_mount/on_unmount）、全局键盘、交易动作
│   │                      #   —— 面板由 `components` 声明为子组件；会变的值以**取值 Proc** 下发
│   ├── engine.rb          # 行情引擎：随机游走 + 涨跌停 + 成交量 + K 线聚合（含可复现 RNG）
│   ├── account.rb         # 交易账户：T+1 / 整手 / 佣金印花税 / 限价撮合 / 冻结资金 / 权益曲线
│   ├── indicators.rb      # SMA / EMA / Wilder RSI / 最大回撤 / 波动率
│   ├── format.rb          # 金额千分位、涨跌色、交易时段与档位换算
│   ├── num.rb             # 数值工具：`Num = Citrine::Num` 的别名（F12/F13 已由框架提供实现）
│   ├── telemetry.rb       # 埋点：Effect 重跑计数（包装框架内部，无官方钩子 → F9 仍未解决）
│   ├── test_api.rb        # 仅剩两件外挂：渲染计数 + 无头验收 API（原 browser_glue.rb）
│   ├── parity.rb          # 跨平台一致性输出脚本（由 rake parity 驱动）
│   └── views/             # 八个面板 + 四个行组件（12 个 Citrine::Component 子类，F5 已落地）
│       ├── common.rb      #   面板构件 + 视图层纪律（重要，改视图前先读）
│       ├── header.rb      #   Header：顶部时段 / 账户总览 / 运行控制
│       ├── watchlist.rb   #   Watchlist + WatchRow（key=股票代码）
│       ├── chart.rb       #   Chart：蜡烛/分时（取样粒度 = 面板自己的 state）
│       ├── ticket.rb      #   Ticket：下单草稿 = 面板自己的 state
│       ├── positions.rb   #   Positions + PositionRow（key=股票代码）
│       ├── logs.rb        #   Logs + TradeRow / OrderRow（key=成交号/委托号）
│       ├── stats.rb       #   Stats：账户统计 + 权益曲线
│       └── debug.rb       #   DebugBar：信号与渲染埋点
├── test/
│   ├── market_test.rb     # CRuby 内核单测
│   └── market_stub_check.js # Node DOM 桩验收
├── FRICTION.md            # ★ 用本应用撞出来的 citrine 改进清单（23 条）
└── docs/                  # 独立审计报告 + 可复现探针与原始输出
```

## 三层验证（为什么敢说它是对的）

| 层 | 手段 | 覆盖 |
|---|---|---|
| 内核 | `rake test`（CRuby + minitest） | 引擎可复现性、涨跌停夹紧、K 线聚合、指标数学、T+1、佣金/印花税、限价撮合与冻结资金、胜率与回撤 |
| 装配 | `rake stubs`（Node DOM 桩） | 挂载、每档渲染开销量化、**响应式属性（行情/换股不重建节点，DOM 身份不变）**、**keyed 复用（排序重排后行仍是同一批节点、插入/删除行不动其它行）**、**面板隔离（面板内部状态更新不重建兄弟面板）**、输入框 DOM 身份与草稿、市价/限价下单、撤单、排序、暂停变速、快捷键、自动交易、清仓、重置 |
| 一致性 | `rake parity` | 同一份内核在 CRuby 与 Opal 下输出逐字节一致（51 行）——专治"单测全绿、浏览器乱码"的跨平台语义差异 |

浏览器侧另有实测（Chrome，`bin/dev` 起服务）：8 个面板与 10 行自选/40 根蜡烛正常渲染、
控制台零报错零异常；市价买入 500 股 → 同档卖出被 T+1 拒绝 → 次档卖出 200 股成交并结算盈亏；
限价挂单冻结 136,906.22 → 撤单解冻；蜡烛/分时切换与粒度切换正常；
埋点面板实时显示"**每档 140 次 Effect 重跑 / 3 个新建 DOM 节点**"（同 seed 同档位实测，
组件嵌套 + keyed 复用迁移前后逐项相同：换股/排序/面板切页都不再重建任何节点）。

最近一次迁移（组件嵌套 + keyed 复用）另用 **headless Chrome 做了脚本化真机验证**：
排序重排后 10 行仍是同一批 DOM 对象、数量输入框仍是同一节点、`document.activeElement`
仍是输入框、草稿值仍在；图表切"分时"后同样如此。
真机截图还抓出一处桩测不出来的布局事故（日志表被排成一行），详见 FRICTION.md 7.6 ⑥。

> 注意：**Node 桩全绿不代表布局正确**。本仓库开发中曾出现"面板子块默认横排、
> 图表塌成 2px"而 60 项桩断言全绿的状况（见 FRICTION.md F23）；
> 组件嵌套迁移时又重演了一次（98 项桩断言全绿，真机截图才看出日志表被排成一行，见 7.6 ⑥）
> ——尺寸类问题只能在真实浏览器里量。

## 业务规则（按 A 股现货惯例简化）

- **整手交易**：数量须为 100 股整数倍，单笔上限 100 万股
- **T+1**：当日买入的批次当日不可卖（按批次记录买入档位，逐个解锁）
- **费用**：佣金万 2.5（最低 5 元），卖出加收千 0.5 印花税
- **限价单**：挂单时按「限价 × 数量 ×(1+佣金率)」冻结资金，成交/撤单解冻；
  价格触及即成交，成交价取「限价」与「市价」中对委托方更有利者
- **涨跌停**：±10%（相对昨收），限价单超出区间直接拒单
- **行情**：几何布朗运动 + 波动聚集 + 小概率跳空；成交量随波动放大；
  随机源是自实现的 MINSTD（`app/engine.rb`），**同 seed 在 CRuby 与 Opal 下逐位一致**

## 已知取舍

- 行情为**随机生成**，与真实市场无关；界面上有显著声明
- 未做：K 线拖拽/缩放、盘口深度、多账户、持久化（刷新即重置）
- 图表用 `div` 而非 Canvas：Citrine v1 的渲染器不能混用（见 FRICTION.md F8）

## 许可

MIT（与 citrine 一致）
